import React from "react";
import { Spin } from "antd";

// Loader component displays a spinning animation to indicate loading.
const Loader = () => (
  <div className="loader">
    <Spin />
  </div>
);

export default Loader;
